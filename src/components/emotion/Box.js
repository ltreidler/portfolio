import styled from "@emotion/styled";

const Box = styled.div({
  display: "flex",
  padding: "1rem",
  backgroundColor: "white",
  zIndex: 1,
  flexWrap: "wrap",
  maxWidth: "85%",
});

export default Box;
